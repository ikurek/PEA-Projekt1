pub mod dynamic_programing_solution {
    use std::time::Instant;

    pub fn solve(matrix : &Vec<Vec<i32>>) {

        //Inicjalizacja smiennej czasowej
        let now = Instant::now();
        unimplemented!();
    }
}